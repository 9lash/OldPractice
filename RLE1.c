// This program implements Running length Algorithm. 
// User inputs number of elements to be entered (n) in buffer,
// Then a string like "wwwwwaaa111233"
// The output should be: "w5a3132132". 

#include<stdio.h>
#include<string.h>

char* encode(char*str)
{
	int len, count, i, j;
	char*dest[];
	len = strlen(str);
	count =0;
	i=j=0;
	while(i<len)
	{
		if(i==0)
		{
			
		}
	}
}

int main()
{
	
char string[256];
//int n,i;	
// User enters the numbers which are to be sent into RLE 	
printf("Enter the number of characters in the buffer you would enter: ");
scanf("%s", string);




 while(getch()!=0); 
}
